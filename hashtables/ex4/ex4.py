# Determine which positive entries in a list of numbers also have corresponding negative numbers.
# ex [ 1, -1, 2, 3, -4, -3, 4, -5, 6, 7 ] should output [ 1, 3, 4 ] because each have a corresponding negative value in the list.  7 does not, so it is excluded, so it 6, and so on.

def has_negatives(a):
    """
    YOUR CODE HERE
    """
    # Your code here

    return result


if __name__ == "__main__":
    print(has_negatives([-1, -2, 1, 2, 3, 4, -4]))
